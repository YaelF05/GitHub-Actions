name: Prueba

on:
  push:
jobs:
  Prueba:
    name: Prueba
    runs-on: ubuntu-latest
    steps:
      - name: Repo
        uses: actions/checkout@v6
      - name: Set up
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Probar
        run: mvn test

